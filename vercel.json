{
  "version": 2,
  "builds": [
    {
      "src": "api/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ],
  "buildCommand": "pip install -r requirements.txt && python manage.py migrate --noinput --settings=config.settings.vercel && python manage.py collectstatic --noinput --settings=config.settings.vercel"
}
